<form method="post" action="{{ path('demande_adhesion_delete', {'id': demande_adhesion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_adhesion.id) }}">
    <button class="btn">Delete</button>
</form>
