{% extends 'base.html.twig' %}

{% block title %}Bureau{% endblock %}

{% block body %}
    <h1>Bureau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bureau.id }}</td>
            </tr>
            <tr>
                <th>Mandat</th>
                <td>{{ bureau.mandat }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ bureau.createdAt ? bureau.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ bureau.updatedAt ? bureau.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bureau_index') }}">back to list</a>

    <a href="{{ path('bureau_edit', {'id': bureau.id}) }}">edit</a>

    {{ include('bureau/_delete_form.html.twig') }}
{% endblock %}
