{% extends 'baseAdmin.html.twig' %}

{% block title %}village
{% endblock %}

{% block body %}
	<div
		class="content-page">
		<!-- Start content -->
		<div class="content">
			<div class="container-fluid">
				<h1>Apropos</h1>

				<table class="table">
					<tbody>
						<tr>
							<th>Id</th>
							<td>{{ apropo.id }}</td>
						</tr>
						<tr>
							<th>Contenue</th>
							<td>{{ apropo.contenue }}</td>
						</tr>
						<tr>
							<th>CreatedAt</th>
							<td>{{ apropo.createdAt ? apropo.createdAt|date('Y-m-d H:i:s') : '' }}</td>
						</tr>
						<tr>
							<th>UpdatedAt</th>
							<td>{{ apropo.updatedAt ? apropo.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
						</tr>
					</tbody>
				</table>

				<a href="{{ path('apropos_index') }}">back to list</a>

				<a href="{{ path('apropos_edit', {'id': apropo.id}) }}">edit</a>

				{{ include('admin/apropos/_delete_form.html.twig') }}
				<!-- end row -->
			</div>
			<!-- container-fluid -->
		</div>
		<!-- content -->
		{{ include('includes/_footer_admin.html.twig')}}
	</div>
{% endblock %}
