{% extends 'baseAdmin.html.twig' %}

{% block title %}Versement{% endblock %}

{% block body %}
    <h1>Versement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ versement.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ versement.montant }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ versement.objet }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ versement.description }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ versement.prenom }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ versement.createdAt ? versement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ versement.updatedAt ? versement.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('versement_index') }}">back to list</a>

    <a href="{{ path('versement_edit', {'id': versement.id}) }}">edit</a>

    {{ include('admin/versement/_delete_form.html.twig') }}
{% endblock %}
