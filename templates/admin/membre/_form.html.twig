{{ form_start(form) }}
<div class="form-row">
	<div class="col">
		{{form_row(form.nom)}}
	</div>
	<div class="col">
		{{form_row(form.prenom)}}
	</div>
</div>
<div class="form-row">
	<div class="col">
		{{form_row(form.ville)}}
	</div>
	<div class="col">
		{{form_row(form.contact)}}
	</div>
</div>
<div class="form-row">
	<div class="col">
		{{form_row(form.profession)}}
	</div>
	<div class="col">
		{{form_row(form.email)}}
		<span style="font-weight: bold;; background: #fff;color:blue;padding:10px;">
			{{email}}
		</span>
	</div>
</div>
<div class="form-row align-items-center">
	<button class="btn btn-primary">{{ button_label|default('Sauvegarder') }}</button>
</div></div>{{ form_end(form) }}
