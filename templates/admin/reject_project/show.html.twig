{% extends 'baseAdmin.html.twig' %}

{% block title %}RejectProject{% endblock %}

{% block body %}
    <h1>RejectProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reject_project.id }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ reject_project.motif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reject_project_index') }}">back to list</a>

    <a href="{{ path('reject_project_edit', {'id': reject_project.id}) }}">edit</a>

    {{ include('admin/reject_project/_delete_form.html.twig') }}
{% endblock %}
