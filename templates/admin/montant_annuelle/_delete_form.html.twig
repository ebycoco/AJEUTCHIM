<form method="post" action="{{ path('montant_annuelle_delete', {'id': montant_annuelle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ montant_annuelle.id) }}">
    <button class="btn">Delete</button>
</form>
