{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Video
{% endblock %}

{% block body %}
	<div
		class="content-page">
		<!-- Start content -->
		<div class="content">
			<div class="container-fluid">
				<div class="page-title-box">
					<div class="row align-items-center">
						<div class="col-sm-6"></div>
						<div class="col-sm-6">
							<a class="btn btn-warning" href="{{ path('video_index') }}">Retour</a>


						</div>
					</div>
					<!-- end row -->
				</div>

				{{ include('admin/video/_form.html.twig', {'button_label': 'Update'}) }}
			</div>
			<!-- container-fluid -->
		</div>
		<!-- content -->
		{{ include('includes/_footer_admin.html.twig')}}
	</div>

{% endblock %}
