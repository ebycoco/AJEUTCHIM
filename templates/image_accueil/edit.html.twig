{% extends 'base.html.twig' %}

{% block title %}Edit ImageAccueil{% endblock %}

{% block body %}
    <h1>Edit ImageAccueil</h1>

    {{ include('image_accueil/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('image_accueil_index') }}">back to list</a>

    {{ include('image_accueil/_delete_form.html.twig') }}
{% endblock %}
