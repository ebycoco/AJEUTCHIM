{% extends 'base.html.twig' %}

{% block title %}Mandat{% endblock %}

{% block body %}
    <h1>Mandat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mandat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mandat.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ mandat.createdAt ? mandat.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ mandat.updatedAt ? mandat.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mandat_index') }}">back to list</a>

    <a href="{{ path('mandat_edit', {'id': mandat.id}) }}">edit</a>

    {{ include('mandat/_delete_form.html.twig') }}
{% endblock %}
