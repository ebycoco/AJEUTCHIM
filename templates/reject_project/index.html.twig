{% extends 'base.html.twig' %}

{% block title %}RejectProject index{% endblock %}

{% block body %}
    <h1>RejectProject index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Motif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reject_project in reject_projects %}
            <tr>
                <td>{{ reject_project.id }}</td>
                <td>{{ reject_project.motif }}</td>
                <td>
                    <a href="{{ path('reject_project_show', {'id': reject_project.id}) }}">show</a>
                    <a href="{{ path('reject_project_edit', {'id': reject_project.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('reject_project_new') }}">Create new</a>
{% endblock %}
