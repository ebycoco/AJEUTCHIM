{% extends 'base.html.twig' %}

{% block title %}EvenementRealiser index{% endblock %}

{% block body %}
    <h1>EvenementRealiser index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>EventedAt</th>
                <th>ImageName</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evenement_realiser in evenement_realisers %}
            <tr>
                <td>{{ evenement_realiser.id }}</td>
                <td>{{ evenement_realiser.title }}</td>
                <td>{{ evenement_realiser.description }}</td>
                <td>{{ evenement_realiser.eventedAt ? evenement_realiser.eventedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ evenement_realiser.imageName }}</td>
                <td>{{ evenement_realiser.createdAt ? evenement_realiser.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ evenement_realiser.updatedAt ? evenement_realiser.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('evenement_realiser_show', {'id': evenement_realiser.id}) }}">show</a>
                    <a href="{{ path('evenement_realiser_edit', {'id': evenement_realiser.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('evenement_realiser_new') }}">Create new</a>
{% endblock %}
